apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-node-ingress
  namespace: myargo
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-node-service
            port:
              number: 8082
